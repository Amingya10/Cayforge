[build]
builder = "RAILPACK"
buildCommand = "npm install && npx prisma generate"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
